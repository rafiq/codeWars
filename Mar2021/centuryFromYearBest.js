// Here is the best and it is simple. I can't believe that it is that easy.
const century = year => Math.ceil(year/100)

// Another pretty clever one as well.
function century(year) {
    return (year + 99) / 100 | 0;
  }function century(year) {
    return (year + 99) / 100 | 0;
  }